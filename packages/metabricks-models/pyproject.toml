[project]
name = "metabricks-models"
version = "0.1.0"
description = "Public metadata-facing Pydantic models for metabricks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
]

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"
