# metabricks-models

Standalone public models package for metadata configuration in metabricks.

## Scope

This package is intended to contain only public metadata-facing models.

Excluded from this package:
- Internal runtime-only models such as `metric_model`

## Install

```bash
pip install metabricks-models
```
